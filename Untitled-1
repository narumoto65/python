#coding:utf-8
import csv
input = ['wakamatsu.csv','maruyama.csv','saitou.csv','fukui.csv','kasuya.csv','kawano.csv']
#d = open('text.txt','w')
#f = open('hoge.csv','rb')
dataReader=csv.reader(f)
for row in dataReader:
    chars= list(row)
    for i in range(len(row)):
        if chars[i].isdigit():
            d.write(chars[i])
            d.write(',')
    d.close
            #print(chars[i])